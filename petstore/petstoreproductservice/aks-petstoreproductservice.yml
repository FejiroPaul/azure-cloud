apiVersion: apps/v1
kind: Deployment
metadata:
  name: aks-petstoreproductservice
spec:
  replicas: 1
  selector:
    matchLabels:
      app: aks-petstoreproductservice
  template:
    metadata:
      labels:
        app: aks-petstoreproductservice
    spec:
      nodeSelector:
        agentpool: petstorenp2
      containers:
      - name: aks-petstoreproductservice
        image: paulazurepetstorecr.azurecr.io/petstoreproductservice:latest
        ports:
        - containerPort: 80
        env:
        - name: PETSTOREPRODUCTSERVICE_SERVER_PORT
          value: "80"
        - name: APPLICATIONINSIGHTS_CONNECTION_STRING
          valueFrom:
            secretKeyRef:
              name: application-insights
              key: connection-string

        - name: APPLICATIONINSIGHTS_ROLE_NAME
          value: "petstore-product-service"
---
apiVersion: v1
kind: Service
metadata:
  name: aks-petstoreproductservice 
spec:
  type: ClusterIP
  ports:
  - port: 80
  selector:
    app: aks-petstoreproductservice
